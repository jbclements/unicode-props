#lang setup/infotab

(define name "unicode")

(define blurb '((p "This package provides definitions for several "
                   "unicode character properties and an auto-extract "
                   "mechanism that can generate additional ones.")))

(define scribblings '(("unicode.scrbl" () (interop))))
(define version "2013-05-07-15:26")
(define release-notes '((p "initial release")))

